import axios from "axios"

export const UpdateCart = async({items})=> {
    console.log(items);
    // const newItems = {
    //   "id": items.id,
    //   "pic": items.pic,
    //   "name": items.name,
    //   "unitprice": items.unitprice,
    //   "quantity": count,
    //   "date": items.date
    // }
    // console.log("items");
    // const res = await axios.put(`http://localhost:313/row/:${items?.id}`,items)
    // const res = await axios.put(`http://localhost:313/row/:${items?.id}`,items)
    // console.log(res);
}